export default function IdeasPage() {
  return (
    <div className="mx-auto max-w-2xl px-4 py-16">
      <h1 className="font-display text-4xl italic">Post ideas</h1>
      <p className="mt-4 text-muted">
        After a round with about a dozen X replies, LiveRound will distill standalone posts in your
        words. You approve the exact text. That is the only server-side publish path — original posts,
        never replies.
      </p>
    </div>
  );
}
